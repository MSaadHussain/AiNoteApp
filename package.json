{
  "name": "notenest-ai-php",
  "version": "1.0.0",
  "private": true,
  "description": "Build pipeline for NoteNest AI (PHP Edition) front-end assets",
  "scripts": {
    "build:css": "tailwindcss -i ./resources/css/input.css -o ./public/assets/css/app.css --minify",
    "watch:css": "tailwindcss -i ./resources/css/input.css -o ./public/assets/css/app.css --watch",
    "build:icons": "node ./resources/build/copy-vendor.mjs",
    "build": "npm run build:icons && npm run build:css",
    "dev": "npm run build:icons && npm run watch:css"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "lucide": "^0.544.0",
    "tailwindcss": "^3.4.17"
  }
}
